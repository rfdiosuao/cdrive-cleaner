# 架构设计文档对齐状态

本文用于跟踪当前代码与《C盘清理项目高性能架构设计文档》的落地差距。设计文档代表目标态，当前项目版本为 `0.2.x`，以可运行、可验证、逐步收敛为原则推进。

## 已对齐

- 四层方向：前端表现层、Tauri command、Rust service/core engine、Windows/SQLite 适配层已经形成基本边界。
- 扫描模式：已有快速扫描、深度扫描、增量扫描入口。
- 实时反馈：扫描和清理均有进度状态、百分比和取消能力。
- 安全防护：已有系统目录保护、扩展名保护、隐私目录保护、签名校验入口。
- 清理闭环：已有预览、备份、清理、恢复点、进度查询和取消能力。
- 审计闭环：清理预览、执行、结果、恢复、取消已写入加密审计日志，并提供只读查询命令。
- AI 接入：已有云端模型配置、连接测试和扫描结果抽样分析框架。
- 插件雏形：已有内置插件信息、启停、注册和注销能力。

## 部分对齐

- 高性能扫描：当前是异步批量遍历加并发限制，但还没有达到文档中的分块多进程扫描、严格流式低内存和性能基准。
- USN 增量扫描：已有 Windows USN 读取代码，但 checkpoint 尚未持久化，增量扫描尚未成为主流程能力。
- 文件指纹缓存：SQLite 表和 `FingerprintCache` 已有，但扫描引擎尚未接入。
- 本地 AI：项目包含 `ort` 依赖，但尚未加载本地 ONNX 模型进行真实推理。
- 插件化：当前是内置插件清单和 Rust trait，尚未支持第三方插件热插拔、市场和沙箱运行。
- 安全执行：已有备份和恢复点，但尚未创建 Windows 系统还原点，也没有真正沙箱预执行。

## 未对齐

- 性能指标缺少基准测试：尚未证明 2TB 全量扫描、增量扫描、内存占用、CPU 占用等指标。
- CLI 工具缺失：文档中的 `cleaner-cli migrate`、`config` 等命令尚未实现。
- 用户行为学习未落地：已有权重更新接口雏形，但没有完整行为记录和自动调参闭环。
- 审计日志页面缺失：后端已可查询，前端还没有审计查看页面。
- 自动清理任务未接入系统计划任务或后台调度。

## 下一步优先级

1. 接入 `FingerprintCache` 到扫描引擎，先支持重复文件和二次扫描缓存命中。
2. 持久化 USN checkpoint，新增“增量扫描”前端入口。
3. 增加 `benchmark` 命令或脚本，输出扫描耗时、文件数、峰值内存、清理耗时。
4. 增加审计日志页面，展示清理记录、风险等级、释放空间和恢复点。
5. 整理后端中文乱码，统一 UTF-8 文案和错误消息。
6. 评估本地 ONNX 模型加载路径，明确本地 AI 的真实边界。
